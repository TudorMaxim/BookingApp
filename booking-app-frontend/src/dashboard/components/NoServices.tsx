import { FunctionComponent } from 'react';

const NoServices: FunctionComponent = () => (
  <div className="no-services">
    <h1> Unfortunatelly, no services are available at the moment. </h1>
    <h2> Be the first to add a new service! </h2>
  </div>
);

export default NoServices;
